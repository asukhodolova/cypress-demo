{
  "name": "cypress-demo",
  "version": "1.0.0",
  "description": "Cypress Sample Tests",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --headed --spec cypress/e2e/*/*.cy.js",
    "test:basic": "npx cypress run --headed --spec cypress/e2e/basic.cy.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0"
  },
  "dependencies": {
    "@zebrunner/javascript-agent-cypress": "^1.0.45",
    "cypress-xpath": "^2.0.1"
  }
}
